# Diagrama de Arquitectura - Proyecto Reuniones

## 📊 Arquitectura Completa

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              CLIENTE                                     │
│                         (Browser, cURL, etc)                            │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             │ HTTP Requests
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      NGINX LOAD BALANCER                                │
│                         (Puerto 80)                                      │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │  Configuración:                                                 │    │
│  │  - Round-robin load balancing                                  │    │
│  │  - Health checks automáticos                                   │    │
│  │  - Timeout: 60 segundos                                        │    │
│  │  - Keepalive: 32 conexiones                                    │    │
│  └────────────────────────────────────────────────────────────────┘    │
└──────┬────────────────────────────────────────────┬─────────────────────┘
       │                                            │
       │ /api/tasks                                 │ /api/tasks
       │ /health                                    │ /health
       │                                            │
       ▼                                            ▼
┌──────────────────────┐                  ┌──────────────────────┐
│  TASKS SERVICE 1     │                  │  TASKS SERVICE 2     │
│    (Réplica 1)       │                  │    (Réplica 2)       │
│                      │                  │                      │
│  Framework: NestJS   │                  │  Framework: NestJS   │
│  Puerto: 3001        │                  │  Puerto: 3001        │
│  Instancia: A1       │                  │  Instancia: A2       │
│                      │                  │                      │
│  Endpoints:          │                  │  Endpoints:          │
│  - GET /api/tasks    │                  │  - GET /api/tasks    │
│  - POST /api/tasks   │                  │  - POST /api/tasks   │
│  - PATCH /api/tasks  │                  │  - PATCH /api/tasks  │
│  - DELETE /api/tasks │                  │  - DELETE /api/tasks │
│  - GET /health       │                  │  - GET /health       │
│                      │                  │                      │
└──────────┬───────────┘                  └──────────┬───────────┘
           │                                         │
           │                                         │
           └─────────────────┬───────────────────────┘
                             │
                             │ MongoDB Connection
                             │
                             ▼
                    ┌─────────────────┐
                    │  MONGODB TASKS  │
                    │                 │
                    │ Puerto: 27017   │
                    │ DB: tasks-db    │
                    │                 │
                    │ Collections:    │
                    │ - tasks         │
                    │                 │
                    │ Volumen:        │
                    │ mongodb-tasks   │
                    └─────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                     ANALYTICS SERVICE (Servicio B)                      │
│                            Puerto: 3002                                  │
│                                                                          │
│  Framework: NestJS                                                      │
│  Comunicación: HTTP (Síncrono)                                          │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │  Patrones Implementados:                                        │    │
│  │                                                                  │    │
│  │  🔄 RETRY PATTERN                                               │    │
│  │     - 3 reintentos automáticos                                  │    │
│  │     - Backoff exponencial: 1s → 2s → 4s                        │    │
│  │     - Timeout: 5 segundos por petición                          │    │
│  │                                                                  │    │
│  │  🔌 CIRCUIT BREAKER                                             │    │
│  │     - Estados: CLOSED → OPEN → HALF_OPEN                       │    │
│  │     - Threshold: 3 fallos consecutivos                          │    │
│  │     - Reset timeout: 30 segundos                                │    │
│  │     - Success threshold: 2 éxitos                               │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  Endpoints:                                                             │
│  - GET /api/analytics/stats                                             │
│  - GET /api/analytics/tasks-by-priority                                 │
│  - GET /api/analytics/circuit-breaker                                   │
│  - POST /api/analytics/circuit-breaker/reset                            │
│  - GET /health                                                          │
│                                                                          │
└────────────────────────────┬───────────────────────────────────────────┘
                             │
                             │ HTTP Request
                             │ (Con Retry + Circuit Breaker)
                             │
                             ▼
                    ┌─────────────────┐
                    │  NGINX LB       │
                    │  (Puerto 80)    │
                    └────────┬────────┘
                             │
                             ▼
                    Tasks Service 1 o 2
```

### Circuit Breaker - Máquina de Estados

```
           Inicio
             │
             ▼
      ┌────────────┐
      │   CLOSED   │ ◄──────────────┐
      │  (Normal)  │                │
      └─────┬──────┘                │
            │                       │
            │ 3 fallos              │ 2 éxitos
            │ consecutivos          │ en HALF_OPEN
            │                       │
            ▼                       │
      ┌────────────┐                │
      │    OPEN    │                │
      │ (Bloquea)  │                │
      └─────┬──────┘                │
            │                       │
            │ 30 segundos           │
            │                       │
            ▼                       │
      ┌────────────┐                │
      │ HALF_OPEN  │ ───────────────┘
      │  (Prueba)  │
      └────────────┘
            │
            │ 1 fallo
            │
            └──────────┐
                       ▼
                 ┌────────────┐
                 │    OPEN    │
                 │            │
                 └────────────┘
```

## 📦 Contenedores Docker

## 📦 Contenedores Docker

```
┌───────────────────────────────────────────────────────────────────────────┐
│                      Docker Compose Network                                │
│                    (microservices-network)                                 │
│                                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                   │
│  │  zookeeper   │  │    kafka     │  │  kafka-init  │                   │
│  │  Port: 2181  │  │  Port: 9092  │  │  (one-time)  │                   │
│  └──────────────┘  └──────────────┘  └──────────────┘                   │
│                                                                            │
│  ┌────────────────────┐  ┌────────────────────┐                          │
│  │ mongodb-tasks      │  │ nginx-lb           │                          │
│  │ Image: mongo:7     │  │ Custom Image       │                          │
│  │ Port: 27017        │  │ Port: 8080         │                          │
│  └────────────────────┘  └────────────────────┘                          │
│                                                                            │
│  ┌────────────────────┐  ┌────────────────────┐                          │
│  │ tasks-service-1    │  │ tasks-service-2    │                          │
│  │ Custom Image       │  │ Custom Image       │                          │
│  │ Internal Port:3001 │  │ Internal Port:3001 │                          │
│  │ + Kafka Producer   │  │ + Kafka Producer   │                          │
│  └────────────────────┘  └────────────────────┘                          │
│                                                                            │
│  ┌────────────────────┐                                                   │
│  │ analytics-service  │                                                   │
│  │ Custom Image       │                                                   │
│  │ Port: 3002         │                                                   │
│  │ + Kafka Consumer   │                                                   │
│  └────────────────────┘                                                   │
│                                                                            │
└───────────────────────────────────────────────────────────────────────────┘
```

## 🔄 Kafka Event-Driven Architecture

### Kafka Topics Creados

```
┌─────────────────────────────────────────────────────────────┐
│                    Kafka Broker (kafka:9092)                 │
│                                                              │
│  Topics:                                                     │
│  ┌────────────────────────────────────────────────────┐    │
│  │  📌 task-events          (3 partitions)           │    │
│  │  📌 task-created         (2 partitions)           │    │
│  │  📌 task-updated         (2 partitions)           │    │
│  │  📌 task-deleted         (2 partitions)           │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  Retention: 24 horas (86400000 ms)                          │
│  Replication Factor: 1                                      │
└─────────────────────────────────────────────────────────────┘
```

### Flujo de Eventos

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Event Flow (Event-Driven)                         │
└─────────────────────────────────────────────────────────────────────────┘

    Usuario
      │
      │ POST/PATCH/DELETE
      ▼
  ┌────────────────┐
  │  NGINX Load    │
  │  Balancer      │
  └────────┬───────┘
           │
           ▼
  ┌──────────────────┐        ┌──────────────────┐
  │ Tasks Service 1  │   OR   │ Tasks Service 2  │
  │                  │        │                  │
  │ 1. Save to DB    │        │ 1. Save to DB    │
  │ 2. Publish Event │        │ 2. Publish Event │
  └────────┬─────────┘        └────────┬─────────┘
           │                           │
           │         Kafka             │
           └───────────┬───────────────┘
                       │
                       ▼
              ┌────────────────┐
              │  Kafka Broker  │
              │                │
              │  Topics:       │
              │  - task-created│
              │  - task-updated│
              │  - task-deleted│
              └────────┬───────┘
                       │
                       │ Subscribe & Consume
                       │
                       ▼
              ┌────────────────────┐
              │ Analytics Service  │
              │                    │
              │ Kafka Consumer:    │
              │ - Recibe eventos   │
              │ - Actualiza stats  │
              │ - Mantiene cache   │
              └────────────────────┘
```

### Comunicación Dual (Síncrona + Asíncrona)

El proyecto implementa AMBOS patrones de comunicación:

**1. Comunicación Síncrona (HTTP)**
```
Analytics Service  ──[HTTP + Retry + Circuit Breaker]──►  NGINX LB  ──►  Tasks Service
    
Endpoints: 
- GET /api/analytics/stats/sync
- GET /api/analytics/tasks-by-priority
```

**2. Comunicación Asíncrona (Kafka Events)**
```
Tasks Service  ──[Kafka Producer]──►  Kafka Topics  ──[Kafka Consumer]──►  Analytics Service

Endpoints:
- GET /api/analytics/stats/event-driven
- GET /api/analytics/events
```

**3. Endpoint Combinado**
```
GET /api/analytics/stats
  ├── Obtiene stats via HTTP (con resiliencia)
  └── Obtiene stats via Kafka (event-driven)
```

## 🎯 Comparación de Patrones

| Aspecto | HTTP (Síncrono) | Kafka (Asíncrono) |
|---------|----------------|-------------------|
| **Acoplamiento** | Fuerte | Débil |
| **Latencia** | Inmediata | Eventual |
| **Resiliencia** | Retry + Circuit Breaker | Topic persistence |
| **Consistencia** | Fuerte | Eventual |
| **Escalabilidad** | Limitada | Alta |
| **Uso en proyecto** | Consultas en tiempo real | Estadísticas y eventos |

````
